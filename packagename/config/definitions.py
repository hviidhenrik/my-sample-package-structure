from pathlib import Path

PATH_CONFIG = Path(__file__).parent
PATH_SRC = PATH_CONFIG.parent  # optionally, change this to reflect the name of the 'packagename' directory
PATH_PROJECT_ROOT = PATH_SRC.parent
